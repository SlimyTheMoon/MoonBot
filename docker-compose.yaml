services:
  moonbot:
    build: .
    container_name: moonbot
    restart: always
    environment:
      - DISCORD_TOKEN=your_token_here
      - DB_PATH=/data/moonbot.db
    volumes:
      - ./data:/data
    # For Web UI expansion later
    ports:
      - "8080:8080"